import { ScrollEfectDirective } from './scroll-smooth.directive';

describe('ScrollEfectDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollEfectDirective();
    expect(directive).toBeTruthy();
  });
});
